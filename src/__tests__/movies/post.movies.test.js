const { addMovie } = require('../../services/movies');

test('service addMovie should add movies for the premium user', () => {
    expect(true).toBe(true);
});

test('should restrict basic user to limited allowed movies', () => {
    expect(true).toBe(true);
});

test('service addMovie should add movies for the premium user', () => {
    expect(true).toBe(true);
});

